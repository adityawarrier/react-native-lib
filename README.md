# A sample react native library

- This library has some dummy react-native ui component and some non ui utilities for POC sake.
- This libary makes use of typescript and exports types for all elements.
